MSI STUCK?  (i3 /health already OK)
=================================

i3 ir gatavs. MSI neredz = WiFi ceļš vai vecs skripts.

I3:
  cd ~/VS-new/VS
  git pull origin main
  sudo bash FORCE_I3_LAN

MSI (TIKAI no C:\VS-main):
  cd /d C:\VS-main
  git pull origin main
  echo 192.168.0.10> ADMIN\config\SERVER_IP.txt
  ADMIN\CONNECT_FORCE.bat

CONNECT_FORCE dara ping+curl. Ja curl FAIL:
  - tā PATI WiFi kā i3 (ne Guest)
  - router: izslēdz AP/Client Isolation
  - VAI ethernet
  - VAI WireGuard → echo 10.77.0.1> ADMIN\config\SERVER_IP.txt
