# Legacy review archive

All historical / obsolete VS material lives here:

- Former `apps/`, `libs/` (C++), `cmake/`, old docs/tests/tools
- Nested historical freeze trees
- Old Windows `VS.exe` / `VS.bat` launchers

**Production MUST NOT import or execute anything from this tree.**

Enforced by `TESTS/unit/no-legacy-ui-imports.test.ts`.

Authoritative products:

| Role | Path |
|------|------|
| Server | `SERVER/` |
| Admin UI | `ADMIN/desktop/` |
| Client UI | `CLIENT/desktop/` |
