# Legacy UI

The previous Control Panel lives at `apps/dashboard` (frozen for reference).

Production UIs are:
- `SERVER/dashboard-v2`
- `ADMIN/apps/dashboard-v2`
- `CLIENT/apps/client-v2`

Do not import this tree from production v2 apps.
