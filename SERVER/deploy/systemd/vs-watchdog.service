[Unit]
Description=VS CORE watchdog (restarts supervisor/service if down)
After=vs-core.service

[Service]
Type=oneshot
ExecStart=/bin/systemctl is-active --quiet vs-core.service || /bin/systemctl restart vs-core.service

[Install]
WantedBy=multi-user.target
