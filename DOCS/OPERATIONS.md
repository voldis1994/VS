# Operations

Daily:

```bash
sudo bash SERVER/STATUS_SERVER
sudo bash SERVER/install/HEALTHCHECK.sh
sudo bash SERVER/SHOW_DASHBOARD.sh
```

ADMIN: `START_ADMIN.bat`  
Backup: `SERVER/install/BACKUP_SERVER.sh`  
Update: `SERVER/install/UPDATE_SERVER.sh` (operator-gated)

LIVE trading stays off until explicit operator verification.
