# VS Admin — native desktop

Python 3 + PySide6 operator console for VS-CORE-01.

Production artifact: `ADMIN/windows/dist/VS Admin.exe` built by `ADMIN/windows/BUILD_ADMIN.bat`.

This directory is not a web app. It does not listen on TCP.
