exit_policy:
  horizon_ms: 10000
  # Keep ≥65% of MFE — PeakProtect (live desk may use tighter SCALP 72%)
  peak_retention_threshold: 0.65
  remaining_rr_threshold: 0.5
  time_decay_seconds: 1.0
  thesis_failure_enabled: true
  reversal_evidence_enabled: true
  actions:
    - HOLD
    - EXIT_NOW
    - TRAIL
    - TAKE_PROFIT
